# Copy this file to .env (for local `vercel dev`) and fill in your key.
# In production, set GEMINI_API_KEY in your Vercel project settings instead
# (Project -> Settings -> Environment Variables) — never commit real keys.

# Free key from Google AI Studio: https://aistudio.google.com/apikey
GEMINI_API_KEY=
